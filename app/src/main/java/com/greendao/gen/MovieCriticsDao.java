package com.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import model.MovieCritics;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "MOVIE_CRITICS".
*/
public class MovieCriticsDao extends AbstractDao<MovieCritics, Long> {

    public static final String TABLENAME = "MOVIE_CRITICS";

    /**
     * Properties of entity MovieCritics.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Name = new Property(1, String.class, "name", false, "NAME");
        public final static Property Critics = new Property(2, String.class, "critics", false, "CRITICS");
        public final static Property CreateTime = new Property(3, java.util.Date.class, "createTime", false, "CREATE_TIME");
        public final static Property Stars = new Property(4, int.class, "stars", false, "STARS");
    }


    public MovieCriticsDao(DaoConfig config) {
        super(config);
    }
    
    public MovieCriticsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"MOVIE_CRITICS\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"NAME\" TEXT NOT NULL ," + // 1: name
                "\"CRITICS\" TEXT NOT NULL ," + // 2: critics
                "\"CREATE_TIME\" INTEGER NOT NULL ," + // 3: createTime
                "\"STARS\" INTEGER NOT NULL );"); // 4: stars
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"MOVIE_CRITICS\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, MovieCritics entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getName());
        stmt.bindString(3, entity.getCritics());
        stmt.bindLong(4, entity.getCreateTime().getTime());
        stmt.bindLong(5, entity.getStars());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, MovieCritics entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getName());
        stmt.bindString(3, entity.getCritics());
        stmt.bindLong(4, entity.getCreateTime().getTime());
        stmt.bindLong(5, entity.getStars());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public MovieCritics readEntity(Cursor cursor, int offset) {
        MovieCritics entity = new MovieCritics( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // name
            cursor.getString(offset + 2), // critics
            new java.util.Date(cursor.getLong(offset + 3)), // createTime
            cursor.getInt(offset + 4) // stars
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, MovieCritics entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setName(cursor.getString(offset + 1));
        entity.setCritics(cursor.getString(offset + 2));
        entity.setCreateTime(new java.util.Date(cursor.getLong(offset + 3)));
        entity.setStars(cursor.getInt(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(MovieCritics entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(MovieCritics entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(MovieCritics entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
